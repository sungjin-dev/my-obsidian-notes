자바스크립트 ES 모듈(ESM)은 `export`한 변수에 대해 라이브 바인딩(Live Binding)을 지원합니다.

이 개념을 바탕으로 **[기초 타입 + 함수 제어]** 방식과 **[참조 타입(객체) 직접 수정]** 방식을 비교해 드릴게요.

## 1. 두 방식의 코드 구조 비교

### 방식 A: 기초 데이터 타입 + 변경 함수 (`increment()`)

JavaScript

```
// counter.js
export let count = 0;

export function increment() {
  count++; // 모듈 내부에서 직접 변경 (라이브 바인딩으로 외부에도 반영)
}
```

### 방식 B: 참조 데이터 타입 (객체)

JavaScript

```
// counter.js
export const state = {
  count: 0
};
```

## 2. 장단점 비교

### 방식 A: 기초 타입 + 변경 함수 (`increment()`)

- **장점 1: 캡슐화(Encapsulation)와 무단 수정 방지 (최대 장점 ⭐)**
    
    - 가져온 쪽(`import`)에서 `count = 100`처럼 **외부에서 값을 직접 수정하려고 하면 자바스크립트가 에러(Read-only)를 발생**시킵니다.
        
    - 오직 `counter.js`가 제공하는 `increment()` 함수를 통해서만 값이 바뀌므로, **데이터의 통제권이 모듈 내부**에 남게 됩니다.
        
- **장점 2: 디버깅 및 디바운스/로그 추가 용이**
    
    - 값이 언제, 어디서 바뀌었는지 추적하고 싶다면 `increment()` 함수 안에 `console.log()`나 조건문을 넣으면 됩니다.
        
- **단점 1: ESM 라이브 바인딩에 대한 이해 필요**
    
    - "기초 타입인데 다른 파일에서 변경된 값이 반영된다고?" 하고 모듈 라이브 바인딩 동작 방식을 잘 모르는 동료가 보면 헷갈릴 수 있습니다.
        
- **단점 2: 구조 분해 할당(Destructuring) 시 연동 끊김**
    
    - `import { count } from './counter.js'`로 가져온 뒤 `let myCount = count`처럼 다른 변수에 할당해 버리면 라이브 바인딩 연결이 끊어집니다.
        

### 방식 B: 참조 데이터 타입 (객체)

- **장점 1: 가장 직관적이고 친숙함**
    
    - 자바스크립트 기본 객체 참조(Reference) 원리 그대로 작동하므로, 라이브 바인딩 원리를 몰라도 누구나 쉽게 이해합니다.
        
- **장점 2: 연관된 여러 상태를 묶기 편함**
    
    - `{ count: 0, name: 'counter', isLoading: false }`처럼 여러 데이터를 하나의 객체로 묶어 전달하기 좋습니다.
        
- **단점 1: 상태 오염 위험 (최대 단점 ⚠️)**
    
    - `import`한 곳 어디서든 `state.count = 9999`처럼 **모듈 주인의 허락 없이 마음대로 값을 조작**할 수 있습니다.
        
- **단점 2: 추적 불가능한 버그 발생**
    
    - 어느 파일의 몇 번째 줄에서 `state.count`를 바꿨는지 추적하기가 매우 힘들어집니다. (사이드 이펙트 관리 불가)
        

## 3. 한눈에 보는 비교표

|**비교 항목**|**방식 A: 기초 타입 + 함수 (increment())**|**방식 B: 참조 타입 객체 (state.count)**|
|---|---|---|
|**외부에서 직접 수정**|❌ **불가능** (에러 발생)|⭕ **가능** (위험)|
|**데이터 수정 통제권**|**모듈 본인**이 소유|**외부 아무나** 가능|
|**디버깅 / 추적**|🟢 **쉬움** (함수 내부만 보면 됨)|🔴 **어려움** (모든 파일 수색해야 함)|
|**동작 방식 원리**|ESM **Live Binding**|JS **Object Reference**|

## 4. 실무에서는 어떤 방법이 더 선호될까?

**결론부터 말씀드리면, 방식 A(함수를 통한 제어) 또는 객체이더라도 메서드로 감싸는 방식이 압도적으로 선호됩니다.**

소프트웨어 공학의 대원칙 중 하나인 "단방향 데이터 흐름"과 **"캡슐화"** 때문입니다.

- **가장 안 좋은 패턴:** `export const state = { count: 0 }`를 넘기고, 외부에서 `state.count++`로 직접 수정하는 것.
    
- **실무에서 사용하는 표준 패턴:**
    
    1. **기초 타입 + 모듈 함수:** 질문하신 `let count = 0` + `export function increment()` 방식
        
    2. **객체 + Getter/Setter 메서드:**
        
        JavaScript
        
        ```
        const state = { count: 0 };
        export const getCount = () => state.count;
        export const increment = () => { state.count++; };
        ```
        
    3. **전태 관리 라이브러리 (Redux, Zustand, Vuex, Pinia 등):** 전부 "상태 변경은 지정된 함수(Action/Dispatch)를 통해서만 일으킨다"는 이 원칙을 바탕으로 만들어져 있습니다.
        

> **💡 요약하자면**
> 
> 데이터(상태)를 모듈 밖으로 내보낼 때는 **"읽기는 허용하되, 수정은 오직 내가 제공하는 함수로만 해라"**가 가장 안전하고 권장되는 프로그래밍 스타일입니다!