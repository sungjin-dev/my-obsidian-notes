백엔드 및 데이터베이스 설계에서 자주 혼동되는 **DTO**, **VO**, **Entity** 삼총사의 차이점과 역할을 명확히 정리해 드릴게요.

## 1. 한 눈에 보는 비교표

| **구분**               | **DTO (Data Transfer Object)**  | **VO (Value Object)**                   | **Entity (엔티티)**                  |
| -------------------- | ------------------------------- | --------------------------------------- | --------------------------------- |
| **핵심 역할**            | 계층 간 데이터 **전송용 상자**             | 값 자체를 표현하는 **불변 객체**                    | **DB 테이블과 1:1 매핑**되는 핵심 데이터       |
| **가변성 (Mutability)** | **가변 (Mutable)** (값 변경 가능)      | **불변 (Immutable)** (Read-Only, 값 변경 불가) | **가변 (Mutable)** (비즈니스 로직에 의해 변경) |
| **식별자 (ID)**         | 식별자 의미 없음                       | 식별자 없음 (값이 같으면 같은 객체)                   | **PK(기본키) 필수 존재** (ID가 다르면 다른 객체) |
| **비즈니스 로직**          | 없음 (Getter/Setter만 존재)          | **포함 가능** (값 검증 및 계산 로직)                | **포함 가능** (핵심 도메인 로직)             |
| **주요 사용 위치**         | Controller ↔ Service, API 응답/요청 | 도메인 로직 내 특정 값 단위                        | Service ↔ Repository (DB 통신)      |


## 2. 각 개념별 정밀 설명

### 1) Entity (엔티티 / Model)

- **정의:** 실제 **데이터베이스(DB)의 테이블과 직접 연결되는 객체**입니다.
    
- **특징:**
    
    - 반드시 유일한 식별자(**PK, Primary Key**)를 가집니다.
        
    - DB 생명주기와 함께하며, 함부로 컨트롤러(화면) 레이어까지 넘기면 안 됩니다. (보안 및 DB 구조 노출 위험)
        

Java

```
// Entity 예시 (JPA / Java 기준)
public class UserEntity {
    private Long id; // DB Primary Key
    private String email;
    private String password; // 🔒 민감 정보!
}
```

### 2) DTO (Data Transfer Object)

- **정의:** 계층(Layer) 간에 데이터를 **안전하고 효율적으로 주고받기 위한 전송용 데이터 상자**입니다.
    
- **특징:**
    
    - 화면(UI) 요구사항이나 API 규격에 맞춰 필요한 필드만 포함합니다.
        
    - `Entity`에서 비밀번호 같은 민감한 정보를 빼고 화면에 전달할 때나, 클라이언트가 보내는 요청 파라미터를 담을 때 씁니다.
        

Java

```
// DTO 예시 (화면 응답용 - 비밀번호 제외)
public class UserResponseDto {
    private Long id;
    private String email;
    // password 필드는 포함하지 않음!
}
```

### 3) VO (Value Object)

- **정의:** 시스템에서 **특정한 '값(Value)' 그 자체를 표현하는 불변(Immutable) 객체**입니다.
    
- **특징:**
    
    - 한번 생성되면 내부 값을 절대로 바꿀 수 없습니다. (Setter 없음)
        
    - 두 VO의 속성 값이 모두 같다면 두 객체는 **완전히 같은 것**으로 취급합니다. (예: `Money(10000원)` 객체 두 개는 서로 다른 객체라도 '10,000원'이라는 같은 가치를 가짐)
        

Java

```
// VO 예시 (위도/경도를 하나로 묶는 위치 값 객체)
public class LocationVO {
    private final double latitude;
    private final double longitude;

    public LocationVO(double latitude, double longitude) {
        this.latitude = latitude;
        this.longitude = longitude;
    }
    // Getter만 존재하고 Setter는 없음 (불변)
}
```

## 3. 전체적인 흐름 (어떻게 함께 사용될까?)

Plaintext

```
 [ Client (브라우저) ]
        │  ▲
        │  │  1. Client ➔ Controller: Request DTO 사용
        ▼  │  4. Controller ➔ Client: Response DTO 사용
  [ Controller ]
        │  ▲
        │  │  2. Controller ➔ Service: DTO 전달
        ▼  │
    [ Service ]  <--- (내부에서 값 계산 시 VO 활용)
        │  ▲
        │  │  3. Service ➔ Repository: Entity 조작 및 변환
        ▼  │
   [ Repository ]
        │  ▲
        ▼  │
    [ Database ]
```

## 💡 한 줄 요약

- **Entity:** DB 테이블 그 자체 (절대 화면까지 함부로 노출시키지 않음)
    
- **DTO:** 레이어 사이를 이동하는 **택배 상자** (화면에 필요한 것만 골라 담음)
    
- **VO:** 변경 불가능한 **값 자체** (예: 주소, 돈, 좌표 등)


**DAO** (Data Access Object, 데이터 접근 객체)는 데이터베이스(DB)의 데이터에 접근하기 위한 객체로, **DB 연동 처리를 전단하는 객체**입니다.

이전에 다룬 Entity, DTO, Service와 함께 백엔드 아키텍처의 핵심 축을 담당하며, **비즈니스 로직과 DB 접근 로직을 분리**하기 위해 사용됩니다.

## 1. DAO의 핵심 역할

- **DB CRUD 작업 전담:** `CREATE(INSERT)`, `READ(SELECT)`, `UPDATE`, `DELETE` 등 SQL을 직접 실행하거나 DB와 직접 통신합니다.
    
- **DB 커넥션 관리:** DB 연결(Connection)을 맺고 해제하는 복잡한 과정을 비즈니스 로직(Service) 대신 처리해 줍니다.
    
- **비즈니스 로직과의 분리:** Service 계층은 DB에 데이터가 어떻게 저장되는지(SQL 문법 등) 알 필요 없이, 단순히 `dao.getUser(id)`처럼 메서드만 호출하면 됩니다.
    

## 2. DTO, Entity, DAO의 전체 관계

이전에 다룬 개념들과 함께 연결해 보면 전체적인 데이터 흐름이 완성됩니다.

Plaintext

```
[ Client ]
   │  ▲
   │  │  (DTO: 요청/응답 데이터)
   ▼  │
[ Controller ]
   │  ▲
   │  │  (DTO)
   ▼  │
[ Service ]   <--- 비즈니스 로직 처리
   │  ▲
   │  │  (Entity / VO)
   ▼  │
  [ DAO ]     <--- DB 접근 로직 전담 (SQL 실행)
   │  ▲
   ▼  │
[ Database ]
```

## 3. 코드 예시 (Java / JDBC 기준)

Java

```
// DAO 예시
public class UserDAO {
    
    // DB에서 사용자 조회 (SELECT)
    public UserEntity getUserById(String userId) {
        String sql = "SELECT * FROM users WHERE id = ?";
        
        // 1. DB 연결 및 SQL 실행
        // 2. 결과를 UserEntity 객체로 변환하여 반환
        return userEntity;
    }

    // DB에 새 사용자 추가 (INSERT)
    public void insertUser(UserEntity user) {
        String sql = "INSERT INTO users (id, name, email) VALUES (?, ?, ?)";
        // DB에 저장 실행
    }
}
```

## 4. DAO vs Repository (참고)

현대 프레임워크(Spring Boot, JPA 등)를 쓰다 보면 DAO 대신 **Repository**라는 용어를 자주 접하게 됩니다.

| **구분**     | **DAO (Data Access Object)** | **Repository (리포지토리)**      |
| ---------- | ---------------------------- | --------------------------- |
| **관점**     | **DB(데이터베이스) 중심**            | **도메인(비즈니스 객체) 중심**         |
| **특징**     | SQL을 직접 작성하거나 테이블 위주로 접근     | 객체 집합(컬렉션)처럼 엔티티를 다룸        |
| **추상화 수준** | 상대적으로 낮음 (DB 조작에 가까움)        | 상대적으로 높음 (JPA 등을 활용해 DB 숨김) |

> 💡 **요약:** 둘 다 **"DB에 접근하는 계층"**이라는 본질은 같으며, SQL 중심 개발에서는 DAO, ORM(JPA 등) 중심 개발에서는 Repository라는 명칭을 주로 씁니다.