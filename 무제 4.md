


## 1. 전통적인 MVC의 문제점: "뚱뚱한 컨트롤러 (Fat Controller)"

순수한 MVC에서는 Controller가 사용자의 요청을 받아 Model에서 데이터를 가져오고, 계산하고, 검증하고, 처리한 뒤 View에 전달했습니다.

하지만 웹 서비스가 커지면서 다음과 같은 문제가 생겼습니다.

Plaintext

```
❌ 서비스 레이어가 없을 때 (Fat Controller):
[ Controller ]
 ├── ① 요청 파라미터 검증
 ├── ② 비즈니스 로직 (포인트 계산, 할인율 적용, 잔액 확인)
 ├── ③ DB 조회 및 수정 명령 (Model 호출)
 ├── ④ 외부 API 호출 (결제 모듈, 카카오 알림톡 전송)
 └── ⑤ 결과 View 선택

➔ Controller 하나에 수백 줄의 코드가 몰리고, 똑같은 비즈니스 로직을 다른 곳에서 재사용하기 불가능해짐!
```

## 2. Service 레이어의 역할: "진짜 비즈니스 로직의 집합소"

Service는 **Controller와 Model(DB) 사이**에 위치하여, 애플리케이션의 핵심 비즈니스 로직(업무 규칙)을 전담합니다.

Plaintext

```
⭕ Service 레이어를 추가한 구조 (Layered Architecture):

 [ Client (브라우저) ]
         │
         ▼
  [ Controller ]  <-- 요청/응답 처리, HTTP 상태코드 제어, 파라미터 검증
         │
         ▼
    [ Service ]   <-- 핵심 비즈니스 로직, 트랜잭션 관리, 외부 서비스 연동
         │
         ▼
  [ Model / DAO ] <-- DB와 직접 통신하여 CRUD 수행
         │
         ▼
   [ Database ]
```

## 3. 계층별 역할 분담 (예시: 계좌 이체 기능)

이해를 돕기 위해 "A 통장에서 B 통장으로 10만 원을 송금한다"는 기능을 예로 들어볼게요.

| **계층**                 | **역할**          | **실제 수행하는 작업**                                                                                                                                                                                  |
| ---------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Controller**         | **요청/응답 전달자**   | • HTTP 요청(POST /transfer) 받기<br><br>  <br><br>• 요청 데이터(보내는 사람, 받는 사람, 금액)가 잘 들어왔는지 1차 검증<br><br>  <br><br>• Service의 `transferMoney()` 메서드 호출<br><br>  <br><br>• 처리 결과를 JSON이나 화면(View)으로 응답    |
| **Service**            | **비즈니스 로직 해결사** | • A의 잔액이 10만 원 이상인지 확인<br><br>  <br><br>• A의 통장에서 10만 원 차감<br><br>  <br><br>• B의 통장에 10만 원 입금<br><br>  <br><br>• **트랜잭션(Transaction) 관리** (하나라도 실패하면 둘 다 취소)<br><br>  <br><br>• 이체 완료 문자/알림톡 발송 |
| **Model (Repository)** | **DB 데이터 조작원**  | • `SELECT * FROM accounts WHERE id = A` (조회)<br><br>  <br><br>• `UPDATE accounts SET balance = ...` (수정)                                                                                        |

## 4. Service를 넣었을 때의 확실한 장점

1. **관심사의 분리 (Separation of Concerns):**
    
    - Controller는 "어떻게 웹 요청을 받아 응답할 것인가"에만 집중합니다.
        
    - Service는 "어떤 비즈니스 규칙으로 데이터를 처리할 것인가"에만 집중합니다.
        
2. **코드 재사용성 증가:**
    
    - 웹 화면에서 송금할 때도 `TransferService.transferMoney()`를 쓰고,
        
    - 모바일 앱 API에서 송금할 때도 똑같은 `TransferService.transferMoney()`를 가져다 쓸 수 있습니다.
        
3. **단위 테스트(Unit Test) 용이성:**
    
    - 웹 서버나 DB를 켜지 않고도 **Service의 비즈니스 로직만 떼어내서 테스트 코드를 작성**하기가 매우 쉬워집니다.
        

## 5. 자바스크립트/Node.js 계열 코드 예시

JavaScript

```
// 1. Controller (요청만 받고 Service로 넘김)
class AccountController {
    constructor(accountService) {
        this.accountService = accountService;
    }

    async transfer(req, res) {
        const { fromAccount, toAccount, amount } = req.body;
        
        // Service 호출
        const result = await this.accountService.transferMoney(fromAccount, toAccount, amount);
        
        return res.status(200).json({ message: "송금 성공", data: result });
    }
}

// 2. Service (비즈니스 로직 담당)
class AccountService {
    constructor(accountRepository) {
        this.accountRepository = accountRepository;
    }

    async transferMoney(fromId, toId, amount) {
        // 비즈니스 로직 1: 잔액 확인
        const sender = await this.accountRepository.findById(fromId);
        if (sender.balance < amount) {
            throw new Error("잔액이 부족합니다.");
        }

        // 비즈니스 로직 2: 송금 처리
        await this.accountRepository.updateBalance(fromId, sender.balance - amount);
        await this.accountRepository.updateBalance(toId, amount);

        return { fromId, toId, amount };
    }
}
```

## 💡 한 줄 요약

> **Service**는 Controller의 짐을 덜어주는 **"실제 일꾼(비즈니스 로직 담당자)"**입니다.
> 
> **Controller**는 손님을 응대하는 **카운터 직원**, **Service**는 주방에서 요리하는 **셰프**, **Model**은 **재고 창고**라고 이해하시면 perfect합니다!



## 1. 모듈이 필요한 이유 (왜 사용할까?)

1. **전역 스코프(Scope) 오염 방지:**
    
    - 여러 JS 파일을 그냥 `<script>` 태그로 불러오면 변수 이름이 겹쳐서 의도치 않은 버그가 자주 터졌습니다.
        
    - 모듈은 **자신만의 독립적인 파일 스코프**를 가지므로 변수 이름이 중복되어도 서로 간섭하지 않습니다.
        
2. **유지보수와 재사용성 향상:**
    
    - 앞서 만드셨던 계좌 관리 시스템처럼 `util.js`, `service.js`, `classes.js` 등으로 기능별로 코드를 나누어 두면, 문제가 생겼을 때 해당 파일만 수정하면 되므로 관리가 훨씬 쉬워집니다.
        

## 2. 자바스크립트 표준 모듈: ESM (ES6 Modules)

자바스크립트(ES6)부터 공식적으로 추가된 모듈 시스템으로, 핵심 문법은 **`export`**(내보내기)와 **`import`**(불러오기)입니다.

### ① 내보내기 (`export`) - 다른 파일에서 쓸 수 있게 허용

JavaScript

```
// 📄 util.js (모듈 파일)

// 1. 하나씩 내보내기 (Named Export)
export function createRandomNumber(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

export const BANK_NAME = 'MyBank';

// 2. 대표로 하나만 내보내기 (Default Export)
export default class Customer {
    constructor(name) {
        this.name = name;
    }
}
```

### ② 불러오기 (`import`) - 내보낸 모듈 가져와서 사용

JavaScript

```
// 📄 main.js (가져와서 쓰는 파일)

// Named Export 가져오기 (중괄호 {} 필요)
import { createRandomNumber, BANK_NAME } from './util.js';

// Default Export 가져오기 (중괄호 없음, 이름 마음대로 지정 가능)
import Customer from './util.js';

console.log(BANK_NAME); // 'MyBank'
const num = createRandomNumber(1, 10);
```

## 3. HTML에서 ES6 모듈 사용하는 방법

HTML 파일에서 자바스크립트 모듈을 불러올 때는 `<script>` 태그에 **`type="module"`** 속성을 반드시 명시해야 합니다.

HTML

```
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Module Test</title>
    <!-- type="module" 필수! -->
    <script type="module" src="./main.js"></script>
</head>
<body>
</body>
</html>
```

> ⚠️ **주의사항 (CORS 에러):**
> 
> `type="module"`을 지정한 파일은 브라우저 보안 정책상 **웹 서버(Live Server, Node.js 서버 등)를 통해서 열어야만 작동**합니다. (`file:///` 경로로 그냥 클릭해서 열면 보안 에러가 생깁니다.)

## 4. 자바스크립트 모듈의 두 종류 (CJS vs ESM)

자바스크립트 생태계에는 크게 두 가지 모듈 방식이 있습니다.

|**구분**|**CommonJS (CJS)**|**ES6 Module (ESM)**|
|---|---|---|
|**주요 사용처**|Node.js 백엔드 기본 환경|최신 브라우저 및 현대 프론트엔드 (React, Vue 등)|
|**문법**|`require()` / `module.exports`|`import` / `export`|
|**동작 방식**|동기식 (파일을 불러올 때까지 멈춤)|비동기식 (효율적인 웹 로딩 지원)|

## 2. 왜 `config` 객체(Reference Type)로 바꿔서 다룰까?

### ① 원시 타입(`export let systemFlag`)을 쓸 때의 문제점

원시 타입 변수는 다른 파일에서 불러와 다룰 때 **상태 변경이 직관적이지 않거나 불가능**할 수 있습니다.

JavaScript

```
// 📄 config.js
export let systemFlag = true;

// 📄 index.js
import { systemFlag } from './config.js';

// systemFlag = false; 
// 💥 에러 발생! import로 가져온 원시 타입 변수는 읽기 전용(Read-only) 연결이므로 
// 가져온 파일 내부에서 직접 값을 재할당할 수 없습니다.
```

### ② 참조 타입(`export const config = { ... }`)으로 바꿨을 때의 장점

`config`라는 하나의 객체(Reference Type)로 묶어두면, **어느 파일에서든 그 객체의 주소를 공유**하게 됩니다. 따라서 `config.systemFlag = false`처럼 내부 속성값을 변경하면, 이 `config` 객체를 참조하고 있는 **모든 파일에 변경된 값이 즉시 반영**됩니다!

JavaScript

```
// 📄 config.js
export const config = {
    systemFlag: true,
    OPENING_NEW_ACCOUNT: 1,
    DO_DEPOSIT: 2,
    DO_WITHDRAW: 3,
    PRINT_DEPOSIT_WITHDRAW_HISTORY: 4,
    SYSTEM_OUT: 99
};

// 📄 index.js
import { config } from './config.js';

// systemFlag 상태를 변경하고 싶을 때
config.systemFlag = false; // ⭕ 가능! (객체의 주소는 그대로이고 내부 속성만 변경됨)

console.log(config.systemFlag); // false
```