$ git switch -c dev origin/dev

  

박성진  [오후 3:45]  

`**pip freeze > requirements.txt**`  
  
결과: 프로젝트 폴더 안에 **requirements.txt 파일**이 생성  
  
대신, 우리가 짠 '소스 코드'와 방금 만든 '쇼핑 리스트(requirements.txt)' 딱 두 가지만 Git에 올림  
  
* 가상환경 폴더는 꼭 **.gitignore** 파일에 등록해서 Git이 무시하도록 설정할 것  
  
이제 다른 컴퓨터에서 git clone으로 각각 프로젝트를 내려받았다고 가정해보면 , 코드는 있지만 모듈이 하나도 없어서 실행하면 에러가 발생하게됨  
  
이때 터미널에 아래 명령어 딱 한 줄만 치면 됨.  
  
`pip install -r requirements.txt`  
  
주의사항 :  
  
가상환경(Virtual Environment) 필수: 이 모든 작업은 반드시 프로젝트 전용 가상환경을 켜둔 상태에서 해야 함  
업데이트 하기: 프로젝트 도중에 새로운 모듈을 pip install로 추가했다면, 잊지 말고 pip freeze > requirements.txt를 한 번 더 실행해서 리스트를 최신화하기



1. 먼저 기준이 되는 dev 브랜치로 이동합니다. `**git checkout dev**`  
  
 2. (기존 작업이 끝난) 로컬 feature 브랜치를 삭제합니다. `**git branch -D feature/trouble**`  
  
 3. 원격 저장소의 최신 상태를 동기화하고 정리합니다. `**git fetch --prune**`  
  
 4. 최신 코드가 반영된 dev 브랜치를 내 로컬로 가져옵니다. (팀원들 소스 병합) `**git pull origin dev**`  
  
 5.  원격 브랜치 삭제   `**git push origin --delete feature/trouble**`  
  
 6.  새 작업을 할 feature 브랜치를 다시 생성합니다. `**git checkout -b feature/trouble**`  
  
(참고자료: `**git config --global fetch.prune true**` git fetch만 쳐도 이후로는  git fetch --prune 으로 처리)  
  
prune 역할 : 원격 저장소의 최신 상태를 가져오면서, **원격에는 이미 지워지고 없는데 내 로컬에 찌꺼기처럼 남아있는** `**origin/...**` **브랜치들을 싹 다 청소** (편집됨)

  

[오후 5:52]

`git reflog`  
  
HEAD@{숫자}: commit: ...  그 숫자 찾기  
  
`git checkout -b recovery-branch`  
  
`git checkout feature/브랜치명`  
  
`git merge recovery-branch`  
  
`git branch -d recovery-branch` (편집됨)