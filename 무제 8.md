데이터 흐름 문제를 진단하고 개선 방안을 제시했습니다

데이터 흐름 문제를 진단하고 개선 방안을 제시했습니다

쭉 봤는데, 구조 자체는 깔끔하게 잘 잡아놨어. 근데 `load_to_db.py`의 `load_series` 쪽에서 하나 걸리는 게 있어서 실제 데이터로 재현까지 해봤어.

**`raw_value`가 항상 NULL로 들어가는 문제**

python

```python
rows.append((r["region"], col, str(r["quarter"]),
             None, float(r[col])))
```

컬럼 순서가 `(region_code, indicator_code, quarter, raw_value, yoy_value)`인데, `raw_value` 자리엔 항상 `None`을 박아놓고, 실제 값은 `yoy_value` 자리로 들어가. 지금 나온 데이터로 그대로 돌려보면:

```
('DJ', 'base_rate', '2020Q1', None, -0.5)
('DJ', 'base_rate', '2020Q2', None, -0.25)
```

문제는 이게 코드만 고친다고 끝나는 게 아니라는 거야 — `collect.py`의 `apply_transform`이 원본값을 변환값으로 **덮어써버려서**, `panel_quarterly.csv` 자체에 이미 raw 값이 안 남아있어. 즉 `load_to_db.py`가 raw_value를 못 채우는 게 아니라, 그 시점엔 이미 채울 재료가 없는 상태야. 스키마는 raw/변환값 둘 다 남기려고 설계됐는데, 파이프라인은 하나만 살아남게 짜여있는 셈이지.

두 가지 중에 고르면 될 것 같아:

- **지금 그대로 두기**: `yoy_value`는 사실 "모형 투입값"이라는 주석이 이미 있으니 diff/level 지표도 거기 넣는 걸 의도한 거라면 문제없어. 대신 `raw_value`는 앞으로도 계속 비어있을 걸 알고 있으면 됨 (아예 컬럼을 빼는 것도 방법).
- **진짜 raw/변환값 둘 다 남기기**: `collect.py`에서 변환 전 값을 버리지 않고 같이 들고 가게 바꿔야 해 (예: 지표당 `_raw`/`_yoy` 두 컬럼으로 내보내기). 이러면 `load_series`도 두 값을 각각 채우게 고쳐야 하고.

원하는 쪽 있으면 그거 기준으로 고쳐줄게.

그 외에 자잘한 것들:

- `idx_series_lookup` 인덱스는 `UNIQUE (region_code, indicator_code, quarter)`가 이미 같은 컬럼으로 인덱스를 자동으로 만들어주기 때문에 사실상 중복이야. 없애도 됨.
- `execute_values(cur, ..., rows)`에서 `rows`가 비어있는 경우(아직 지표가 하나도 안 모였을 때 등) 방어 코드가 없어서, `if rows:` 체크 한 줄 추가해두면 안전할 듯.
- `DATABASE_URL`은 굳이 따로 `export` 안 해도, `.env`에 `ECOS_API_KEY`랑 같이 한 줄 추가해두면 돼 — 이 파일이 `config`를 import하는 순간 `config.py`의 `load_dotenv()`가 같이 실행되거든.