웹 개발(DOM 환경)에서 `this`가 "DOM(HTML 요소)을 가리킨다"는 것은 "현재 이벤트가 발생한 그 HTML 태그 객체 자체를 잡고 있다"는 뜻입니다.

상황별로 쉽게 이해하실 수 있게 정리해 드릴게요!


## 1. 이벤트 리스너 함수 내부에서의 `this` (DOM을 가리킬 때)

앞서 작성하셨던 코드처럼 `addEventListener`의 콜백 함수 내부에서 `this`를 출력하면, 이벤트를 듣고 있는 그 HTML 요소(DOM)가 됩니다.

JavaScript

```
const colorInput = document.querySelector('#colorPicker');

colorInput.addEventListener('input', function() {
    console.log('this ---->', this);
    // 💡 여기서 this는 바로 <input id="colorPicker" type="color"> 태그 자체입니다!
    
    // 따라서 this를 통해 해당 DOM의 속성에 바로 접근할 수 있습니다.
    console.log(this.value); // 선택한 색상 값 (#ff0000 등)
    console.log(this.id);    // 'colorPicker'
});
```

- **의미:** "이 함수를 실행하게 만든 주체(HTML 태그)가 바로 나(`this`)다!"
    
- **활용:** `event.target`을 쓰는 것과 비슷하게, 해당 엘리먼트의 값(`this.value`), 스타일(`this.style.color`), 클래스 등을 직접 변경할 수 있게 됩니다.
    

## 2. ⚠️ 주의! 화살표 함수(`() => {}`)를 쓸 때의 차이점

자바스크립트에서 아주 흔하게 겪는 함정입니다.

함수를 일반 함수(`function() {}`)로 썼을 때와 화살표 함수(`() => {}`)로 썼을 때 `this`가 바뀌어 버립니다.

### ① 일반 함수 사용 시

JavaScript

```
colorInput.addEventListener('input', function() {
    console.log(this); // ⭕ <input id="colorPicker"> (DOM 요소를 가리킴)
});
```

### ② 화살표 함수 사용 시

JavaScript

```
colorInput.addEventListener('input', (e) => {
    console.log(this); // ❌ DOM이 아니라 'Window' (전체 창 객체)를 가리킴!
});
```

> **왜 다를까요?**
> 
> 화살표 함수는 자신만의 `this`를 가지지 않고, **자신을 감싸고 있는 바깥쪽의 `this`**를 그대로 가져오기 때문입니다. 따라서 화살표 함수 내에서 해당 DOM을 가리키고 싶다면 `this` 대신 **`e.target`**을 사용해야 합니다.

## 3. 이벤트 바깥(최상위 스코프)에서의 `this`

이벤트 리스너 안이 아니라, JS 파일 맨 위에 그냥 `console.log(this)`를 찍으면 어떻게 될까요?

JavaScript

```
console.log('this ---->', this); // Window 객체 출력
```

이때의 `this`는 HTML 태그가 아니라 웹 브라우저 전체를 담당하는 `Window` 객체(최상위 전역 객체)를 가리키게 됩니다.

## 💡 한 줄 요약

- **`function()` 안의 `this`** ➔ 나를 호출한 **HTML 태그(DOM)** 그 자체
    
- **`this.value`** ➔ 그 HTML 태그가 가지고 있는 값


## 1. 면접관이 `this`를 물어보는 이유

1. **실행 컨텍스트(Execution Context) 이해도 검증**
    
    자바스크립트의 `this`는 다른 언어(Java, C++ 등)와 달리 "어떻게 선언되었는가"가 아니라 "어떻게 호출되었는가"에 따라 동적으로 결정(동적 바인딩)됩니다. 이 차이를 아는지 확인합니다.
    
2. **실무 버그 대응 능력**
    
    이벤트 리스너, React의 클래스형 컴포넌트, `setTimeout` 등에서 `this` 바인딩이 깨져 `undefined`나 `window`를 참조하는 버그는 신입/경력 불문 자주 터집니다. 이를 예방하고 해결할 수 있는지 봅니다.
    
3. **화살표 함수와의 차이점 파악**
    
    ES6 이후 화살표 함수의 도입으로 `this`의 동작 방식이 변화했는데, 두 방식의 차이와 용도를 정확히 구분하는지 확인합니다.