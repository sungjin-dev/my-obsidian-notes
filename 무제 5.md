$$  
Total\_Loss = (w_1 \times box\_loss) + (w_2 \times cls\_loss) + (w_3 \times dfl\_loss)  
$$

학습별 진행과정 1차 →2차

제트스키 포함 시

![BoxF1_curve.png](attachment:42849877-1eab-4474-8e82-193ea67c6dca:BoxF1_curve.png)

zetski confidence 값이 상대적으로 낮음

![results.png](attachment:76292287-40be-419b-91a7-1b1c6efd0d26:results.png)

- `train`과 `val`(검증)의 `box_loss`, `cls_loss`, `dfl_loss` 그래프를 보면 **기울기가 매우 완만하고 꾸준히 우하향**

- `metrics/mAP50(B)`와 `metrics/mAP50-95(B)`를 보시면, **정체 구간(Plateau) 없이 지속적으로 우상향**

### 1. Loss 그래프 (모델이 '공부'하는 과정)

왼쪽 3개(`box`, `cls`, `dfl`)는 모델이 학습 과정에서 **얼마나 실수를 많이 하고 있는지**를 나타냅니다. 낮을수록 좋습니다.

- **`box_loss` (박스 손실):** 예측한 박스(`x, y, w, h`)가 실제 정답 박스와 얼마나 차이 나는지 측정합니다. 이게 낮을수록 **물체의 위치를 정확히** 잡는다는 뜻입니다.
- **`cls_loss` (클래스 손실):** "이게 사람인가, 자동차인가?" 하는 **분류(Classification)를 얼마나 정확히** 하는지 측정합니다. 이게 낮을수록 물체를 올바르게 구분합니다.
- **`dfl_loss` (DFL 손실):** Distribution Focal Loss의 약자로, 객체의 경계(박스 끝부분)를 더 정밀하게 다듬는 역할입니다. 박스의 정밀도가 높아질수록 낮아집니다.

### 2. Metrics 그래프 (모델의 '성능' 결과)

오른쪽 2개(`precision`, `recall`, `mAP`)는 학습이 끝난 후 **실제로 얼마나 잘 맞추는지**를 보여줍니다. 높을수록 좋습니다.

- **`precision` (정밀도):** 모델이 "이건 물체야!"라고 했을 때, **실제로 진짜 물체일 확률**입니다. 이게 높으면 '헛다리'를 짚는 경우(오탐)가 적어집니다. (존재가 있다고 판단했을 때의 정확도)
- **`recall` (재현율):** 실제 물체가 있는데, 모델이 **놓치지 않고 "이건 물체야!"라고 찾아낼 확률**입니다. 이게 높으면 '놓치는 경우(미탐)'가 적어집니다. (존재 여부 파악)
- **`mAP50` & `mAP50-95`:** 이 두 지표가 가장 중요합니다!
    - **mAP50:** 정답 박스와 예측 박스가 50% 이상 겹치면 '정답'으로 인정할 때의 평균 정확도입니다.
    - **mAP50-95:** 50%부터 95%까지 엄격한 기준을 적용해서 평균을 낸 것입니다. **이 수치가 높을수록 "진짜 똑똑한 모델".**

### 1. 첫 번째 이미지 (일반 혼동 행렬)

- **의미:** 모델이 **실제로 몇 번이나** 예측했는지를 나타내는 '절대적인 숫자'
- **보는 법:**
    - **대각선 (왼쪽 위에서 오른쪽 아래로):** 모델이 정답을 맞힌 횟수. 숫자가 클수록 좋다.
    - **나머지 칸:** 모델이 틀린 횟수. 예를 들어, `jetski` 행과 `boat` 열이 만나는 칸의 숫자가 크다면, 모델이 제트스키를 보트로 오해하고 있다는 뜻.
- **특징:** 이 표를 보면 데이터의 양(Volume)을 알 수 있습니다. `ignored`나 `swimmer` 데이터가 다른 클래스보다 훨씬 많다

## **그림1**

![image.png](attachment:10f5f9ec-8f13-4fb4-8d9f-3da50d804bef:image.png)

### 2. 두 번째 이미지 (정규화된 혼동 행렬 - Normalized)

- **의미:** 숫자를 비율(0~1 사이)로 변환한 것입니다. 실제 데이터 양의 차이를 무시하고 "모델이 얼마나 헷갈려하는지(확률)".
    
- **보는 법:**
    
    - **대각선:** 각 클래스별 정확도(Recall)입니다. `swimmer`는 0.97(97%)로 매우 잘 맞추고 있고, `jetski`는 0.65(65%)로 상대적으로 가장 낮습니다.
- **특징:** 성능 분석할 때 훨씬 유용합니다. 데이터 개수가 적은 클래스와 많은 클래스를 동일선상에서 비교할 수 있기 때문.
    

1. **`jetski`의 낮은 정확도:** 정규화된 행렬에서 `jetski`는 대각선 수치가 `0.65`로 가장 낮습니다. 즉, 10대 중 3~4대는 다른 걸로 인식.
2. **`jetski`가 `background`로 빠지는 문제:** 행렬 하단 `background` 행을 보면 `jetski` 열의 수치가 `0.33`이나 된다. 이는 **모델이 제트스키를 찾았는데 "배경(background)이라고 판단**하거나, 반대로 배경을 제트스키로 잘못 인식하는 경우가 많다는 것을 의미.
3. **병합의 근거:** `jetski` 행을 보면 다른 칸들은 거의 비어있는데, 이는 모델이 제트스키를 보트로 헷갈리기보다는, **제트스키 자체를 잘 찾지 못하거나 배경과 혼동**하고 있음.

### 그림2

![image.png](attachment:25d5d09f-8733-4aa2-8ca2-51964eb81680:image.png)

### 해결 방안

- **병합 추천:** `jetski`의 성능(0.65)이 다른 클래스보다 현저히 낮으므로, **보트와 합쳐서 학습하는 것이 모델의 전체적인 성능 향상에 도움이 될 확률이 높다.**
- **데이터 보강:** `background` 행과 `jetski` 열이 만나는 곳의 숫자가 크다는 것은, 현재 데이터셋에서 제트스키가 배경과 잘 구분되지 않거나 제트스키 데이터 자체가 더 필요하다는 신호.

![image.png](attachment:ae52a6e3-2166-4e37-889b-76708934ecff:image.png)

**결론:** `jetski`의 성능(정확도)이 낮았던 근본적인 이유는 **데이터가 절대적으로 부족했기 때문**

원인분석

1. jetski 정확도가 낮았던 이유는 절대적인 데이터가 부족했음. 배경과 혼동하는 경우가 많음
2. map 상승곡선을 보면 에포크를 상승시켜서 더 좋은 결과를 창출할 가능성이 있음

해결방안 → jetski를 boat class에 병합하여 2차 학습 진행하고 따라서 비슷한 물체인 boat 클래스에 같이 적용시켜 데이터를 증강하고 학습 효과를 증진시키는 방향으로 진행

# **2차 학습 결과**

![results.png](attachment:589ebbe4-0b5d-4f53-91a2-6ec48964fce5:results.png)

1차와 크게 다르지 않음. 즉 제트스키를 보트 클래스에 병합했던 시도는 유의미한 성과는 없음

![confusion_matrix.png](attachment:9c466930-d811-48db-9bb6-49d98767e4a2:confusion_matrix.png)

![confusion_matrix_normalized.png](attachment:92cc7358-8e9a-43c5-8568-6781f0dce5aa:confusion_matrix_normalized.png)

제트스키가 클래스에서 제거난 이후 구명장비와 배경을 혼동하는 사례가 발생

→ 기존 제트스키의 문제점이 구명장비로 전이되는 현상 발생

![labels.jpg](attachment:b28b13f9-c304-416b-88fd-97b440b62599:labels.jpg)

제트스키와 마찬가지로 절대적인 데이터 수가 적음.

**3차 학습 방향성**

불필요한 데이터 삭제 진행 → 에포크 70 patience 50으로 진행. 학습 반복을 통해 map증진이 가능한지 여부 파악 8900 이미지 → 7600 이미지로 변경

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
   7/70      2.57G      1.694     0.7806     0.8609        126        640: 100% ━━━━━━━━━━━━ 480/480 5.1it/s 1:34
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 240/240 3.5it/s 1:09
               all       7675      50877      0.838      0.735      0.758       0.44
```

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
   8/70      2.57G      1.677     0.7624     0.8573        110        640: 100% ━━━━━━━━━━━━ 480/480 5.2it/s 1:33
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 240/240 3.5it/s 1:08
               all       7675      50877      0.828      0.731      0.749      0.422
```

```
  Epoch     results.csvGPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
   9/70      2.57G      1.648     0.7451     0.8515        114        640: 100% ━━━━━━━━━━━━ 480/480 5.2it/s 1:33
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 240/240 3.4it/s 1:10
               all       7675      50877      0.858      0.743      0.768      0.455
```

확률적 경사 하강법(SGD)의 특성

**지속적인 하락인가?:** 만약 3~4 에포크 연속으로 `mAP`가 계속 떨어진다면 그때는 '과적합(Overfitting)'을 의심

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  29/70      2.57G      1.461      0.626       0.83         66        640: 100% ━━━━━━━━━━━━ 480/480 4.9it/s 1:37
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 240/240 3.4it/s 1:11
               all       7675      50877      0.894      0.815      0.832      *0.528*
```

28포크 만에 0.522 달성 확실히 개선방향이 좋았음.

patience 값을 50을 주었는데 에포크가 51을 초과함

이말인 즉슨 아직도 학습성과가 개선될 여지가 충분하다는 것

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  51/70      2.57G      1.322     0.5543     0.8139        229        640: 37% ━━━━╸─────── 181/480 5.1it/s 36.7s<58.2s
```

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  50/70      2.57G      1.329     0.5592     0.8136         85        640: 100% ━━━━━━━━━━━━ 480/480 5.2it/s 1:33
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 240/240 3.4it/s 1:11
               all       7675      50877       0.91      0.834      0.852      0.563
```

지금 map 값이 0.563 초과함

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  55/70      2.57G      1.303     0.5409     0.8101        102        640: 100% ━━━━━━━━━━━━ 480/480 5.4it/s 1:30
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 240/240 3.7it/s 1:05
               all       7675      50877      0.915      0.836      0.857      0.572
```

$$  
\text{Total Loss} = (7.5 \times \text{box\_loss}) + (0.5 \times \text{cls\_loss}) + (1.5 \times \text{dfl\_loss})  
$$

![BoxF1_curve.png](attachment:b7270c32-500a-41aa-bac6-37f7edc49a5b:BoxF1_curve.png)

에포크를 늘리고 불필요한 데이터(10% 삭제)를 정리하고 나서 눈에 띄게 좋아짐.

mAP (mean Average Precision)

Confidence Score

IoU(Intersection over Union) 임계값

mAP@50 : 예측한 박스와 정답 박스의 겹치는 영역(IoU)이 **50% 이상**이면 "정답을 맞혔다"고 간주

mAP@50-95 (mAP@[.5:.95]) : IoU를 **0.50부터 0.95까지 0.05 단위로 10단계**로 나누어 각 점수를 계산한 뒤 평균

따라서 **산업 현장에서는 mAP@50보다는 @50-95 점수를 보고 모델의 최종적인 정교함을 판단**

![results.png](attachment:13a335ad-0632-41ff-9c2e-0814f87dbb86:results.png)

**4차 학습**에서는 ~~class를 추가해보는 방향으로 진행해볼 생각임.~~

~~기상악화 여건에 따른 추가적인 변수에 대응하는 능력을 제고~~

~~눈 비오는 사진이 추가되어야함~~

현재 과적합 징후가 보이지 않고 학습 잠재력이 존재해 추가적으로 에포크를 늘려볼 생각임.

epoch 100 patience 80 진행하고 나서 추후 결정함

**증강옵션 적용해보기**

model.train(data='data.yaml', epochs=100, imgsz=640,  
fliplr=0.5, # 50% 확률로 좌우 반전  
flipud=0.2, # 20% 확률로 상하 반전  
degrees=15, # -15도에서 +15도 사이로 무작위 회전  
perspective=0.001) # 원근 변환(Perspective Transform) 강도 설정

```python
augment=True,        # 증강 활성화
        degrees=15.0,        # 회전 적용
        perspective=0.001,   # 원근 변환 적용
        shear=5.0,           # 기울기 적용
        fliplr=0.5           # 좌우 대칭
```

**위기 발생

data.yaml 에서 값이 수정되지 않고 val 이미지 경로가 잘못되어 있었음.**

```yaml
train: images/train    
val: images/train --->   # 수정 후   val: images/val
```

|**구분**|**Train 데이터셋**|**Val (Validation) 데이터셋**|
|---|---|---|
|**목적**|모델의 가중치 학습 (**공부**)|모델 성능 평가 및 튜닝 (**모의고사**)|
|**정답지 공개**|예 (학습에 사용)|아니오 (평가용으로만 사용)|
|**학습 영향**|학습 데이터로 직접 반영됨|학습 종료 시점 결정에 기여|
|**이상적인 상태**|충분히 다양하고 양이 많아야 함|모델이 배운 패턴을 검증하기 적절해야 함|

지금까지 모의고사를 풀지 않은 상황임

지금 현재 val 추가 + 증강효과+에포크까지 늘어난 상황이라 학습효과를 판단하기 까다로움

이후 변수들을 재조정해서 최적의 결과를 얻어야 함

또 한번의 위기 중간에 학습이 중단되는 사태 발생!

```python
# model = YOLO('yolov8n.pt') 
    model = YOLO(r'C:\Alot1team\project\troubleshooting\yolo_trainning\results\my_experiment_v1-3\weights\last.pt')
```

먼저 last.pt값을 기준으로 다시 시작한다. last.pt가 그간 학습데이터를 모두 보존하고 있기 때문

```python
model.train(resume=True,
        data=r'C:\Alot1team\project\troubleshooting\yolo_trainning\data.yaml',
        epochs=100,
        imgsz=640,
```

resume = true 도 적어주고

```python
if __name__ == '__main__':

    model = YOLO(r'C:\Alot1team\project\troubleshooting\yolo_trainning\results\my_experiment_v1-3\weights\last.pt')
    model.train(resume=True)

    start_training()
```

메인 가드(Main Guard) 즉 메인 블록에도 설정을 해주면 된다.

![confusion_matrix.png](attachment:630f4751-76d0-463b-a6f4-0b587744188e:confusion_matrix.png)

![confusion_matrix_normalized.png](attachment:791d5dc2-b38a-4984-a26a-e91fbe4fab88:confusion_matrix_normalized.png)

![labels.jpg](attachment:ba1cf29c-340a-449a-a77c-18c753d952bf:labels.jpg)

![results.png](attachment:7b650b71-87f2-4f64-9823-3a81f14de107:results.png)

데이터셋을 좀 더 정제하여 정교하게 다듬을 필요성이 대두

ignored에 너무 많이 속성이 부과 제거하고 실행해봄.

classes=[1, 2, 3, 4],

해상도도 640→1280으로 조정하여 → imgsz=1280,

작은 물체도 잘 파악할 수 있도록 변경

~~추가목표~~

~~model.train(  
data='...',  
box=10.0, # 기본값보다 높여서 위치 추정에 더 집중하게 함  
dfl=2.0, # 경계선을 더 정교하게 잡고 싶다면 조금 상향

# ... 나머지 설정

)~~

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  1/100      7.48G      2.082      7.247      1.236         48       1280: 0% ──────────── 0/48
  1/100      7.48G      2.054      7.517      1.235         47       1280: 0% ──────────── 1/48
  1/100       7.5G      2.049      8.215      1.328         55       1280: 4% ╸─────────── 23/480 3.2s/it 1:13<24:08
```

속도가 너무 느려 batch를 4로 하향 조정

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  7/100      2.48G      1.594      0.928      1.213         12       1280: 100% ━━━━━━━━━━━━ 1919/1919 7.3it/s 4:24
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 186/186 10.4it/s 17.9s
               all       1484       3169       0.42      0.411      0.418      0.204
```

지금 모자이크 증강(`mosaic=0.5`)이 1280 해상도와 만나면서 **물체들을 너무 잘게 쪼개버리고 있음**

---

---

---

---

---

---

---

현재 val로 모의고사를 세팅해놓고 정작 학습을 안 시켜서 **과적합**된 데이터를 그동안 관찰해왔음

val로 따로 모의고사를 보는게 얼마나 중요한지 깨달음

다시 새로 시작한다는 마음으로 원점에서 시작한다는 마음으로 백업본으로 학습

**제1차 (val 포함 백업본+데이터 정제)**

```python
import os

label_root = r'C:\Alot1team\project\troubleshooting\yolo_trainning\dataset\labels'
img_root = r'C:\Alot1team\project\troubleshooting\yolo_trainning\dataset\images'
min_size = 0.009

for root, dirs, files in os.walk(label_root):
    for label_file in files:
        if not label_file.endswith('.txt'): continue
        
        label_path = os.path.join(root, label_file)
        
        with open(label_path, 'r') as f:
            lines = f.readlines()
        
        for line in lines:
            parts = line.split()
            if len(parts) >= 5:
                w, h = float(parts[3]), float(parts[4])
                
                if w < min_size or h < min_size:
                    # 라벨이 속한 하위 폴더 경로 추출 (예: val 폴더)
                    relative_path = os.path.relpath(root, label_root)
                    current_img_dir = os.path.join(img_root, relative_path)
                    
                    for ext in ['.jpg', '.png', '.jpeg']:
                        img_name = label_file.replace('.txt', ext)
                        img_path = os.path.join(current_img_dir, img_name)
                        
                        if os.path.exists(img_path):
                            print(f"삭제 중: {img_name}")
                            os.remove(img_path)
                            os.remove(label_path)
                            break
                    break
```

너무 작은 물체가 있는 사진들은 과감히 삭제함. 엄청 높은 고도에서 찰영하는게 취지는 아님.

처음에 min_size = 0.005 → 0.009로 많이 삭제해버림.

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  5/100      2.61G      1.704     0.9595     0.9994         27        640: 100% ━━━━━━━━━━━━ 246/246 5.6it/s 44.0s
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 23/23 4.4it/s 5.3s
               all        733       3480      0.881      0.599      0.658      0.379
```

너무 피사체가 작은 물체를 삭제했더니 검증 결과가 좋아짐.

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
  6/100      2.62G      1.691     0.9242     0.9934         42        640: 100% ━━━━━━━━━━━━ 246/246 5.5it/s 45.1s
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 23/23 4.8it/s 4.8s
               all        733       3480      0.793      0.608      0.623      0.352
```

다시 조금 값이 떨어지긴 했으나 관망해보자

```
 Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
 17/100      2.87G      1.514     0.7396      0.953         44        640: 100% ━━━━━━━━━━━━ 246/246 5.6it/s 43.7s
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 23/23 4.2it/s 5.5s
               all        733       3480      0.914      0.667      0.703      0.442
```

굉장히 효과가 좋아 보인다. image수가 val데이터인데 box(p) 값도 0.914로 굉장히 높다

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
 29/100      2.04G      1.418     0.6786     0.9283         27        640: 100% ━━━━━━━━━━━━ 246/246 5.6it/s 44.0s
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 23/23 4.8it/s 4.8s
               all        733       3480      0.914      0.697       0.71      0.448
```

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
 30/100      2.04G      1.415     0.6744     0.9242         39        640: 100% ━━━━━━━━━━━━ 246/246 5.7it/s 43.2s
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 23/23 4.9it/s 4.7s
               all        733       3480       0.79      0.653      0.643        0.4
```

에포크 30대가 넘었음에도 크게 진척이 없고 정체됨 .

# val 제대로 포함한 1차

![BoxF1_curve.png](attachment:7f57e2ad-936a-4af5-8446-0c227c8d4228:BoxF1_curve.png)

![results.png](attachment:8b38273b-6c52-48d5-ad10-ef8ea341eb45:results.png)

![confusion_matrix.png](attachment:22e51ebc-ac01-46ba-87c1-352d350646e1:confusion_matrix.png)

![confusion_matrix_normalized.png](attachment:b8fc1606-8d98-4af7-b8ec-1f32954b144b:confusion_matrix_normalized.png)

![labels.jpg](attachment:1314f2dc-e526-4b04-9a74-ecf926d79411:labels.jpg)

제트스키는 전혀 구분하지 못하고 있음. 클래스 설정을 제대로 하지 못함. 사실상 제트스키가 있는 상황은 긍정적이거나 ignored 와 거의 유사하므로 boat에 병합하기로 결정.

# **2차 jetski를 boat에 병합 후 진행**

```
  Epoch    GPU_mem   box_loss   cls_loss   dfl_loss  Instances       Size
   2/70      2.52G      1.822      1.521      1.027         34        640: 100% ━━━━━━━━━━━━ 246/246 5.7it/s 43.2s
             Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 100% ━━━━━━━━━━━━ 23/23 4.6it/s 5.0s
               all        733       3480      0.746      0.506      0.575      0.317
```

![BoxF1_curve.png](attachment:d8c71743-659c-4018-8998-584b832b5a79:BoxF1_curve.png)

![BoxP_curve.png](attachment:25f30941-b8f5-4066-a4c9-49179b12efc7:BoxP_curve.png)

![BoxPR_curve.png](attachment:18a45d56-8301-471b-8218-e2a158228835:BoxPR_curve.png)

![BoxR_curve.png](attachment:8c0e14e2-7c94-4496-b697-61a748cc82df:BoxR_curve.png)

![confusion_matrix.png](attachment:b0a8aa26-fa68-4931-9576-07b7a36de351:confusion_matrix.png)

![confusion_matrix_normalized.png](attachment:33791c95-921d-4def-a783-3f66c49d1997:confusion_matrix_normalized.png)

![labels.jpg](attachment:572f7af6-1d0d-435b-862e-7bf9a1c9b5a8:labels.jpg)

![results.png](attachment:8b6ffa14-fe32-4deb-bb0f-ba2f62feaad8:results.png)

제트스키를 병합한 이후 오히려 구호 장비를 인식 못하는 현상이 발생 오히려 제트스키를 인식 못하는 편이 낫기 때문에 기존 방식을 유지하는게 좋다고 판단 됨.

핵심은 "현재 프레임에서 탐지되었을 때, 이전에도 연속으로 탐지되었는지 기록하는 변수"를 만드는 것

아까 만든 `while` 반복문 안에 **카운터 변수**만 추가하면 됩니다.

### 연속 프레임 체크 구현 예시

Python

```
from ultralytics import YOLO
import cv2

model = YOLO(r'C:\path\to\your\best.pt')
video_path = r'C:\path\to\your\test_video.mp4'
cap = cv2.VideoCapture(video_path)

# --- 연속성 체크를 위한 변수 설정 ---
consecutive_count = 0    # 연속으로 탐지된 횟수
threshold_count = 5      # 5프레임 이상일 때만 경보
is_alert_active = False  # 현재 경보 중인지 상태 관리
# ----------------------------------

while cap.isOpened():
    ret, frame = cap.read()
    if not ret: break

    results = model(frame, conf=0.3)

    # 이번 프레임에서 사람이 탐지되었는지 확인
    detected_this_frame = False
    for r in results:
        for box in r.boxes:
            # 0번이 'swimmer' 클래스라고 가정할 때
            if int(box.cls) == 0:
                detected_this_frame = True
                break

    # --- 연속성 로직 적용 ---
    if detected_this_frame:
        consecutive_count += 1
    else:
        consecutive_count = 0  # 탐지 안 되면 바로 초기화 (중요!)

    # 5프레임 이상 연속 탐지 시 경보 출력
    if consecutive_count >= threshold_count:
        if not is_alert_active:
            print("🚨 경보 발생: 사람 감지됨!")
            is_alert_active = True
    else:
        is_alert_active = False # 탐지가 끊기면 경보 상태도 해제

    # 결과 보여주기
    annotated_frame = results[0].plot()
    cv2.imshow("Drone Detection Test", annotated_frame)

    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cap.release()
cv2.destroyAllWindows()
```

### 코드 설명

1. **`consecutive_count`**: 탐지될 때마다 +1씩 늘어납니다.
2. **`detected_this_frame`**: 이번 프레임에 사람이 있는지 확인하는 깃발입니다.
3. **`else: consecutive_count = 0`**: 이게 핵심입니다. 만약 사람이 안 보이는 프레임이 딱 한 번이라도 나오면, **연속성 카운트를 0으로 초기화**하여 오탐지를 원천 차단합니다.
4. **`is_alert_active`**: 경보가 매 프레임마다 울리지 않도록, "이미 경보 중이면 또 울리지 마라"는 상태 관리 변수입니다.

### 팁

- **민감도 조절**: 지금은 5프레임이지만, 더 신중하게 하고 싶다면 `10`이나 `15`로 늘리세요.
- **실제 경보**: 나중에 `print` 대신 `winsound.Beep()` 함수를 넣거나, 텔레그램 봇으로 알림을 보내는 코드를 저 자리에 넣으시면 진짜 경보 시스템이 됩니다!

3차 제트스키를 클래스에서 제외하고 학습

4차 에포크를 늘려서 진행

5차 degrees=5, # 회전 적용 하여 마지막 점검 (거의 완료)

6차 degrees=10, 여기까지 하고 마무리할 듯

### 말씀하신 현상 = "Catastrophic Forgetting (치명적 망각)"

가장 정확히 맞아떨어지는 개념이에요.

> **사전학습된 모델에 새로운 클래스를 추가로 학습시키면, 기존에 잘 인식하던 클래스의 성능이 떨어지거나 아예 인식을 못 하게 되는 현상**

### 왜 이런 일이 생길까?

신경망은 학습할 때 **가중치(weight)를 계속 업데이트**하면서 패턴을 학습해요. 그런데 이 가중치는 **모든 클래스가 공유하는 하나의 파라미터 공간**에 저장돼요.

```
기존 학습: [사람, 자동차, 개] 인식하도록 가중치 조정됨   ↓새 클래스 추가 학습: [고양이] 데이터만 넣고 계속 학습   ↓가중치가 "고양이 인식"에 맞게 계속 업데이트됨   ↓"사람, 자동차, 개"를 인식하던 가중치 패턴이 점점 덮어써짐(overwrite)
```

**핵심 원인**: 새 클래스만 학습시키면, 그 손실함수(loss)는 오직 "고양이를 잘 맞추는가"만 신경 써요. 기존 클래스(사람, 자동차, 개)에 대한 성능은 손실함수에 전혀 반영이 안 되니까, 모델 입장에서는 "그 가중치를 유지할 이유가 없어져서" 자연스럽게 흐트러지는 거예요.

### 이게 특히 YOLO 같은 Object Detection에서 두드러지는 이유

YOLO는 마지막 레이어가 **클래스 개수(N)에 딱 맞춰서** 출력 노드가 정해져 있어요.

```
YOLO 출력층: [클래스1 확률, 클래스2 확률, ..., 클래스N 확률, bbox좌표...]
```

**만약 기존 20개 클래스 모델에 새 클래스 1개를 추가**하려면, 출력층 구조 자체를 21개로 바꿔야 하는데, 이때 접근법에 따라 문제가 갈려요:

1. **Fine-tuning으로 새 클래스 데이터만 넣고 재학습** → 위에서 설명한 Catastrophic(kætəˈstrɑːfɪk) Forgetting 발생 위험 높음
2. **기존 데이터 + 새 데이터를 섞어서 처음부터/함께 재학습** → 이 문제를 피할 수 있지만, 기존 데이터셋을 다시 다 가지고 있어야 함 (실무에서 항상 가능한 건 아님)

### 실무에서 이 문제를 완화하는 방법들

- **Fine-tuning 시 기존 데이터도 일부 섞어서 학습** (Rehearsal/Replay 방식)
- **Elastic Weight Consolidation (EWC)**: 기존 클래스에 중요한 가중치는 "덜 변하게" 제약을 거는 기법
- **Knowledge Distillation**: 기존 모델의 출력을 "정답"처럼 활용해서 새 모델이 기존 지식도 함께 유지하도록 학습
- **클래스별로 별도 헤드(head)를 두는 구조** 사용

### 정리

> **말씀하신 현상은 정확히 "Catastrophic Forgetting"이라는, 지속학습(Continual Learning) 분야에서 유명한 난제예요.** 새 클래스만 넣고 학습시키면 기존 클래스에 할당됐던 가중치 표현이 새 클래스 학습 과정에서 밀려나면서 덮어써지는 게 원인

7차 ignored 클래스 없애고 마지막

현재 클래스명이 꼬여서 다시 처음부터 작업하는 중

```python
import json
import os

# 각 데이터셋에 대한 경로 설정
datasets = [
    {
        "json_path": "C:/Alot1team/project/troubleshooting/yolo_trainning/dataset/raw/instances_train.json",
        "output_dir": "dataset/labels/train"
    },
    {
        "json_path": "C:/Alot1team/project/troubleshooting/yolo_trainning/dataset/raw/instances_val.json",
        "output_dir": "dataset/labels/val"
    }
]

def convert(json_file_path, output_txt_dir):
    os.makedirs(output_txt_dir, exist_ok=True)

    print(f"⏳ '{os.path.basename(json_file_path)}' 파일을 읽는 중입니다...")
    try:
        with open(json_file_path, 'r', encoding='utf-8') as f:
            data = json.load(f)
    except FileNotFoundError:
        print(f"❌ 에러: '{json_file_path}' 파일을 찾을 수 없습니다.")
        return

    images_info = {img['id']: img for img in data['images']}

    print(f"✅ 총 {len(images_info)}장의 이미지 정보 확인. 변환을 시작합니다!")

    count = 0
    for ann in data['annotations']:
        img_id = ann['image_id']
        cat_id = ann['category_id'] - 1 
        
        # [수정 위치] 여기서 필터링하고 번호를 조정합니다.
        if cat_id == 0: 
            continue 
        cat_id -= 1 
        
        x_min, y_min, w, h = ann['bbox']

        img_info = images_info[img_id]
        img_w, img_h = img_info['width'], img_info['height']

        x_center = (x_min + (w / 2.0)) / img_w
        y_center = (y_min + (h / 2.0)) / img_h
        w /= img_w
        h /= img_h

        base_name = os.path.splitext(img_info['file_name'])[0]
        txt_filepath = os.path.join(output_txt_dir, f"{base_name}.txt")

        with open(txt_filepath, 'a', encoding='utf-8') as txt_file:
            txt_file.write(f"{cat_id} {x_center:.6f} {y_center:.6f} {w:.6f} {h:.6f}\n")
        count += 1
    
    print(f"🎉 변환 완료! 총 {count}개의 객체가 '{output_txt_dir}'에 저장되었습니다.\n")

if __name__ == "__main__":
    for ds in datasets:
        convert(ds["json_path"], ds["output_dir"])
```

클래스를 0번을 떙기고 했던 작업들….

1차 최종

![BoxF1_curve.png](attachment:904fa094-3894-480f-8c68-966cff7177fa:BoxF1_curve.png)

![BoxP_curve.png](attachment:3233ec0c-518d-47a5-be7f-d920984afa89:BoxP_curve.png)

![BoxPR_curve.png](attachment:d6546c1d-a99f-43c2-8ec3-164cde0eefd1:BoxPR_curve.png)

![BoxR_curve.png](attachment:9c9a5d27-fa20-48a2-9867-30c53c141be2:BoxR_curve.png)

![confusion_matrix.png](attachment:562b8a03-5cb4-4f5e-9dab-bd68bb85c693:confusion_matrix.png)

![confusion_matrix_normalized.png](attachment:dc050e72-6d54-458a-8ea5-95150a2b0015:confusion_matrix_normalized.png)

![labels.jpg](attachment:5ab03079-425f-414d-a7ff-3819f1d3769a:labels.jpg)

![results.png](attachment:76c6a980-423f-4ae2-aae9-ee0a56acab60:results.png)

너무 작아서 안 보이는 객체는 다시 정리함

```python
import os
import shutil
from collections import defaultdict

label_root = r'C:\Alot1team\project\troubleshooting\yolo_trainning\dataset\labels'
img_root = r'C:\Alot1team\project\troubleshooting\yolo_trainning\dataset\images'
min_size = 0.009

dry_run = True

# 원본 라벨 폴더를 통째로 백업해두고 싶으면 True (라벨 파일은 용량이 작아서 권장)
make_backup = True
backup_root = label_root + '_backup'

img_exts = ['.jpg', '.png', '.jpeg']

stats_before = defaultdict(int)
stats_after = defaultdict(int)
removed_lines = 0
removed_images = 0

if make_backup and not dry_run and not os.path.exists(backup_root):
    shutil.copytree(label_root, backup_root)
    print(f"[백업 완료] {backup_root}")

for root, dirs, files in os.walk(label_root):
    for label_file in files:
        if not label_file.endswith('.txt'):
            continue

        label_path = os.path.join(root, label_file)

        with open(label_path, 'r') as f:
            lines = f.readlines()

        kept_lines = []
        for line in lines:
            parts = line.split()
            if len(parts) < 5:
                continue

            cls_id = parts[0]
            w, h = float(parts[3]), float(parts[4])
            stats_before[cls_id] += 1

            if w < min_size or h < min_size:
                removed_lines += 1
                continue  # 이 박스(줄)만 버림

            kept_lines.append(line)
            stats_after[cls_id] += 1

        relative_path = os.path.relpath(root, label_root)
        current_img_dir = os.path.join(img_root, relative_path)

        if len(kept_lines) == 0:
            # 남은 박스가 하나도 없을 때만 이미지+라벨 삭제
            removed_images += 1
            print(f"[이미지 전체 삭제 대상] {label_file} (남은 박스 0개)")
            if not dry_run:
                os.remove(label_path)
                for ext in img_exts:
                    img_path = os.path.join(current_img_dir, label_file.replace('.txt', ext))
                    if os.path.exists(img_path):
                        os.remove(img_path)
                        break
        else:
            # 일부 박스만 걸러낸 경우: 라벨 파일만 다시 쓰기 (이미지는 유지)
            if len(kept_lines) != len(lines):
                print(f"[라인 일부 제거] {label_file}: {len(lines)} -> {len(kept_lines)}")
                if not dry_run:
                    with open(label_path, 'w') as f:
                        f.writelines(kept_lines)

print("\n===== 요약 =====")
print(f"모드: {'DRY RUN (실제 변경 없음)' if dry_run else '실제 적용됨'}")
print(f"제거된 박스(라인) 수: {removed_lines}")
print(f"완전히 삭제된 이미지 수: {removed_images}")
print("\n클래스별 박스 개수 (필터링 전 -> 후):")
for cls_id in sorted(set(list(stats_before.keys()) + list(stats_after.keys())), key=lambda x: int(x)):
    print(f"  class {cls_id}: {stats_before[cls_id]} -> {stats_after[cls_id]}")
```

그럼 ignored를 설정하는것이 마치 null -> "" 이렇게 바뀐거라고도 볼 수 있구나