

setnterval(실행할함수, 시간);

setInterval(function() {...}, 1000)   비동기 방식 

많은 데이터를 모두 동기화하기에는 너무 복잡해진다 

메인페이지를 호출하면 필요한 컴포넌트를 알아서 비동기방식으로 실행 

##  오차 없이 정확한 시간을 다루는 보완 방법

시계, 스톱워치, 카운트다운처럼 **정확한 시간이 중요한 기능**을 만들 때는 타이머의 간격에 의존하지 않고 실제 시각(Timestamp)을 기준점으로 계산해야 합니다.

### 해결책: Absolute Timestamp(현재 시각) 기반 계산

JavaScript

```
const startTime = Date.now(); // 시작 시각 기록

setInterval(() => {
    const currentTime = Date.now();
    // 1초 단위로 늘리는 대신, 실제 경과한 시간을 계산
    const elapsedTime = Math.floor((currentTime - startTime) / 1000);
    
    console.log('정확하게 경과한 초: ', elapsedTime);
}, 1000);
```

이 방식을 사용하면 타이머 실행이 0.1초 밀리거나 백그라운드 탭에 갔다가 돌아오더라도, `Date.now()`로 **실제 흘러간 시각**을 직접 비교하기 때문에 정확한 시간을 유지할 수 있습니다.

setInterval() 과 비슷한 함수 setTimeout()

setTimeout()  



`setInterval()`과 `setTimeout()`은 둘 다 자바스크립트에서 **시간 지연 후 코드를 실행하는 타이머 API**이지만, "반복해서 실행하느냐"와 "단 한 번만 실행하느냐"에서 결정적인 차이가 있습니다.

## 1. 핵심 차이점 비교

| **구분**    | **setTimeout()**                       | **setInterval()**                      |
| --------- | -------------------------------------- | -------------------------------------- |
| **동작 방식** | 지정한 시간이 지난 후 **딱 1번만** 실행              | 지정한 시간 간격(Interval)마다 **무한 반복** 실행     |
| **주 목적**  | 일정 시간 뒤 실행되는 **지연 작업** (예: 3초 뒤 팝업 닫기) | 일정 주기마다 실행되는 **반복 작업** (예: 1초마다 시계 갱신) |
| **취소 함수** | `clearTimeout(timerId)`                | `clearInterval(timerId)`               |


## 2. 코드 비교

### ① `setTimeout()` — 1회성 실행

JavaScript

```
console.log('시작');

// 2000ms(2초) 뒤에 콜백 함수를 단 한 번 실행합니다.
const timeoutId = setTimeout(() => {
    console.log('2초가 지났습니다!');
}, 2000);

// 취소하고 싶다면:
// clearTimeout(timeoutId);
```

- **출력:** 시작 $\rightarrow$ (2초 대기) $\rightarrow$ "2초가 지났습니다!" (이후 자동 종료)
    

### ② `setInterval()` — 주기적 반복 실행

JavaScript

```
let count = 0;

// 1000ms(1초)마다 콜백 함수를 계속해서 반복 실행합니다.
const intervalId = setInterval(() => {
    count++;
    console.log(`1초마다 실행 중... (${count}회)`);
}, 1000);

// 취소하고 싶다면:
// clearInterval(intervalId);
```

- **출력:** "1초마다 실행 중... (1회)" $\rightarrow$ "1초마다 실행 중... (2회)" $\rightarrow$ ... (지속 반복)
